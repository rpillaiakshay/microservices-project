{
  "name": "microservice-app",
  "version": "1.0.0",
  "description": "A microservice application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",  // Start the app in dev mode
    "lint": "eslint .",          // Linting script
    "test": "jest",              // Run tests (you can add Jest later)
    "build": "docker build -t rpillaiakshay/myapp ."  // Build docker image
  },
  "dependencies": {
    "express": "^4.17.1",
    "nodemon": "^2.0.15",        // Added nodemon for development
    "dotenv": "^10.0.0"          // For environment variable management
  },
  "devDependencies": {
    "eslint": "^8.16.0",          // For linting
    "jest": "^27.4.5"             // For testing (if needed)
  }
}
